/*id for android client to use in login with google (this was created in
    google's colsole in credentials for api)*/

const ANDROID_CLIENT_ID = "";
export default {
    androidId: ANDROID_CLIENT_ID
};